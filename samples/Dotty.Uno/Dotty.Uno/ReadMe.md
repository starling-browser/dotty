# Dotty Uno sample

This is a small Uno app that uses `Dotty.Terminal`.

It does not include a full Uno terminal control yet. It shows the basic host flow:

- create a `TerminalSession`
- read text from the session
- show that text in Uno controls
- write user input back to the terminal state