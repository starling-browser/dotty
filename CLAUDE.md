# Dotty - Project Instructions

## UI Requirements

- **All UI controls must be keyboard navigable.** Every interactive element (buttons, toggles, cards, panels, etc.) must support focus, keyboard activation (Enter/Space), and arrow-key or Tab-based navigation as appropriate.
